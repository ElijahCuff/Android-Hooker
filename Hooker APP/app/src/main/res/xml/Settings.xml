<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">
    <PreferenceCategory android:title="Client Settings"/>
        
    
    
    <EditTextPreference
        android:key="client"
        android:title="Device Name"
        android:summary="Name this device for the server to identify"
        android:dialogTitle="Device Name" 
        android:dialogMessage="Please enter the name of this device"
        android:defaultValue="Tester" />
    

    <EditTextPreference
        android:key="hook"
        android:title="Hook Server Address"
        android:summary="@string/hook_server"
        android:dialogTitle="Hook Server Address" 
        android:dialogMessage="Please enter the address of your hook.php script"
        android:defaultValue="@string/hook_server" />
    <Preference
        android:key="token"
        android:title="Device Token"
        android:summary="Set your device name and your hook server address and then click to load the token, it will be registered with your hook server."
    />
    <!--
    <SwitchPreference
        android:key="fullscreen"
        android:title="Fullscreen"
        android:summary="Hide the status bar and fill the whole screen" 
        android:defaultValue="false" />

    
    <Preference
    android:key="basic_preference"
    android:title="basic"
    android:summary="basic pref" />

    <Preference
    android:key="stylish_preference"
    android:title="stylish preference"
    android:summary="this is a stylish preference" />

    <Preference
    android:key="preference_with_icon"
    android:title="Preference with icon"
    android:summary="This preference has an icon"
    android:icon="@drawable/icon" />

    <PreferenceCategory
    android:title="prefsCat">

    <CheckBoxPreference
    android:key="checkbox_preference"
    android:title="checkbox"
    android:summary="checkboxPrefs" />

    <SwitchPreference
    android:key="switch_preference"
    android:title="Switch preference"
    android:defaultValue="true"
    android:summary="This is a switch" />

    <DropDownPreference
    android:key="dropdown_preference"
    android:title="dropdowm"
    android:summary="dropdown prefs"
    android:entries="@array/tst"
    android:entryValues="@array/entryvalues_list_preference" />

    </PreferenceCategory>

    <PreferenceCategory
    android:title="dialogPrefs">

    <EditTextPreference
    android:key="edittext_preference"
    android:title="editText"
    android:summary="editText prefs"
    android:dialogTitle="edit prefs" />

    <ListPreference
    android:key="list_preference"
    android:title="@string/title_list_preference"
    android:summary="@string/summary_list_preference"
    android:entries="@array/entries_list_preference"
    android:entryValues="@array/entryvalues_list_preference"
    android:dialogTitle="@string/dialog_title_list_preference" />

    <MultiSelectListPreference
    android:key="multi_select_list_preference"
    android:title="@string/title_multi_list_preference"
    android:summary="@string/summary_multi_list_preference"
    android:entries="@array/entries_list_preference"
    android:entryValues="@array/entryvalues_list_preference"
    android:dialogTitle="@string/dialog_title_multi_list_preference" />

    </PreferenceCategory>

    <PreferenceCategory
    android:title="launch Prefs">

    This PreferenceScreen tag serves as a screen break (similar to page break
    in word processing). Like for other preference types, we assign a key
    here so it is able to save and restore its instance state. 
    <PreferenceScreen
    android:key="screen_preference"
    android:title="screenPrefs"
    android:summary="screenPrefs pref">

    You can place more preferences here that will be shown on the next screen. 

    <CheckBoxPreference
    android:key="next_screen_checkbox_preference"
    android:title="extra checkbox"
    android:summary="extra checkbox pref" />

    </PreferenceScreen>



    </PreferenceCategory>

    <PreferenceCategory
    android:title="prefAttribs stylised">

    <CheckBoxPreference
    android:key="parent_checkbox_preference"
    android:title="@string/title_parent_preference"
    android:summary="@string/summary_parent_preference" />

    The visual style of a child is defined by this styled theme attribute. 

    <CheckBoxPreference
    android:key="child_checkbox_preference"
    android:dependency="parent_checkbox_preference"
    android:layout="?android:attr/preferenceLayoutChild"
    android:title="childbox"
    android:summary="child checkbox pref" />

    </PreferenceCategory>
    -->
</PreferenceScreen>

